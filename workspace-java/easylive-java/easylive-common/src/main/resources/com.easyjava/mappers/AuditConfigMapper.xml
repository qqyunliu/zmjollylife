<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easyjava.mappers.AuditConfigMapper">
    <resultMap id="base_result_map" type="com.easyjava.entity.po.AuditConfig">
        <result column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="base_column_list">
        id,config_key,config_value,description,create_time,update_time
    </sql>

    <select id="selectAll" resultMap="base_result_map">
        select <include refid="base_column_list"/>
        from tb_audit_config
        order by id
    </select>

    <select id="selectByKey" resultMap="base_result_map">
        select <include refid="base_column_list"/>
        from tb_audit_config
        where config_key = #{configKey}
    </select>

    <update id="updateConfigValue">
        update tb_audit_config
        set config_value = #{configValue}, update_time = now()
        where config_key = #{configKey}
    </update>
</mapper>
