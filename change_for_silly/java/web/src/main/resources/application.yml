server:
  port: 7072
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
  application:
    name: easylive-web
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/easylive?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true&useSSL=false
    username: root
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: HikariCPDatasource
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    timeout: 10000
    jedis:
      pool:
        max-active: 50
        max-wait: 10000
        max-idle: 20
        min-idle: 5
mybatis:
  configuration:
    map-underscore-to-camel-case: true  # 开启下划线转驼峰
  mapper-locations: classpath*:com.easyjava/mappers/*.xml
  type-aliases-package: com.easyjava.entity  # 实体类所在包（与 mapper-locations 同级）

project:
  folder: c:/webser/easylive/
log:
  root:
    level:
      com.easyjava.mappers:DEBUG
      org.apache.ibatis:debug
management:
  endpoints:
    web:
      exposure:
        include: redis

aliyun:
  access-key-id: ${ALIYUN_ACCESS_KEY_ID:}
  access-key-secret: ${ALIYUN_ACCESS_KEY_SECRET:}
  region: ${ALIYUN_REGION:cn-shanghai}

volcengine:
  api-url: ${VOLCENGINE_API_URL:https://ark.cn-beijing.volces.com/api/v3/chat/completions}
  api-key: ${VOLCENGINE_API_KEY:}
  image-model-id: ${VOLCENGINE_IMAGE_MODEL:doubao-seed-2-0-mini-260215}
  audio-model-id: ${VOLCENGINE_AUDIO_MODEL:deepseek-v3-2-251201}